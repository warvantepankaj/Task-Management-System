# Copy to .env.production (or set in your hosting provider's env config).
# Vite inlines these at build time; rebuild + redeploy on change.

VITE_API_BASE_URL=https://api.yourdomain.com
VITE_WS_URL=wss://api.yourdomain.com
